---
title: MCP
description: Connect your preferred MCP-compatible development tool to the s&box docs server.
---

Use MCP when your IDE or agent supports MCP servers.

## 1) Configure your preferred tool

<Tabs
  items={[
    "Visual Studio Code",
    "Cursor",
    "Claude Desktop",
    "Other MCP tools",
  ]}
>
  <Tab>
    Choose one of the following setup options:

    <Accordions defaultValue="vscode-one-click" type="single">
      <Accordion
        title="Option 1: Install with the VS Code deeplink"
        value="vscode-one-click"
      >
        <McpVscodeInstallButton />

        1. Click the install button above.
        2. Approve the VS Code prompt to add the server.
        3. Reload VS Code if the MCP tools do not appear immediately.
      </Accordion>

      <Accordion
        title="Option 2: Paste the config manually"
        value="vscode-manual"
      >
        Add this to your Visual Studio Code MCP settings:

        <McpVscodeConfigSnippet />
      </Accordion>
    </Accordions>

  </Tab>
  <Tab>
    Choose one of the following setup options:

    <Accordions defaultValue="cursor-one-click" type="single">
      <Accordion
        title="Option 1: Install with the Cursor deeplink"
        value="cursor-one-click"
      >
        <McpCursorInstallButton />

        1. Click the install button above.
        2. Approve the Cursor prompt to add the server.
        3. Restart Cursor if the MCP tools do not appear immediately.
      </Accordion>

      <Accordion
        title="Option 2: Paste the config manually"
        value="cursor-manual"
      >
        Add this to your Cursor MCP settings:

        <McpCursorConfigSnippet />
      </Accordion>
    </Accordions>

  </Tab>
  <Tab>
    Choose one of the following setup options:

    <Accordions defaultValue="claude-mcpb" type="single">
      <Accordion
        title="Option 1: Download the .mcpb bundle"
        value="claude-mcpb"
      >
        This installs `sdocs` as a Claude Desktop extension.

        It requires `Node.js 18+` on your machine. The `.mcpb` does not run the
        hosted MCP server directly inside Claude Desktop. Instead, it installs a
        small local Node.js proxy that forwards Claude Desktop MCP traffic to
        the hosted `sdocs` server over HTTP.

        Use this when you want the packaged Claude Desktop install flow. If you
        want Claude Desktop to connect directly to the hosted MCP endpoint
        without the local Node.js proxy, use the Custom Connector option below
        instead.

        <McpMcpbInstallButton />

        1. Download the `.mcpb` bundle.
        2. Make sure `Node.js 18+` is installed locally.
        3. Open the bundle with Claude Desktop.
        4. Approve the install prompt.
        5. Restart Claude Desktop if the tools do not appear immediately.
      </Accordion>

      <Accordion
        title="Option 2: Add Custom Connector (Claude Pro)"
        value="claude-custom-connector"
      >
        Anthropic's "Add Custom Connector" flow is currently limited to Claude
        Pro plans and higher.

        <McpRemoteUrlSnippet />

        1. Open Claude Desktop.
        2. Go to `Settings > Connectors`.
        3. Click `Add custom connector`.
        4. Paste the `sdocs` MCP URL above.
        5. Save and enable the connector in the conversation.
      </Accordion>
    </Accordions>

  </Tab>
  <Tab>
    Choose one of the following setup options:

    <Accordions defaultValue="other-remote-http" type="single">
      <Accordion
        title="Option 1: Register the hosted MCP URL directly"
        value="other-remote-http"
      >
        Use this when your IDE or agent supports adding a remote MCP server by
        URL.

        <McpRemoteUrlSnippet />

        1. Open your tool's MCP or connectors settings.
        2. Add a new MCP server or connector.
        3. Choose the remote HTTP or streamable HTTP transport when prompted.
        4. Paste the `sdocs` MCP URL above.
        5. Save the configuration and restart the tool if required.
      </Accordion>

      <Accordion
        title="Option 2: Paste a JSON config snippet"
        value="other-json-config"
      >
        Some IDEs expect a JSON config instead of a single URL. Use this as a
        starting point and adjust the key name to match your tool:

        <McpOtherToolConfigSnippet />

        If your IDE uses a different transport label such as
        `streamableHttp`, keep the same URL and switch only the transport name.
      </Accordion>
    </Accordions>

  </Tab>
</Tabs>

## 2) Restart your tool

After saving the config, restart your IDE/agent so it can discover the `sdocs` MCP server.

## 3) Verify with a quick prompt

```text
Search for `Sandbox.Component`, inspect its members, then show the exact signature for `OnUpdate`.
```

## Advanced

The MCP server now behaves like a typical SDK documentation server rather than
a generic docs search endpoint. It exposes a small set of structured tools that
let an AI agent progressively discover the s&box API using the standard
workflow:

1. Search
2. Inspect
3. Read details

In practice, an agent will usually:

1. Call `search_docs` to find the most relevant symbols for a question
2. Call `resolve_symbol` if a short name like `Component` or `Scene` is ambiguous
3. Call `explain_symbol_context` for compact symbol metadata, guide handles,
   member handles, and next-tool hints
4. Follow `workflow.next` from compact outputs when deeper context is needed
5. Call `get_symbol` or `get_type_members` for focused API inspection
6. Call `get_method_details` to inspect one exact overload
7. Call `get_related_guides` when broader usage context is recommended
8. Call `get_examples` when sample code is needed
9. Call `list_namespaces` when exploring the API tree from the top down

This makes the s&box documentation MCP feel closer to a .NET API browser, where
namespaces, types, and members are returned in a predictable structure instead
of as raw page text.

## Transport and Payload Format

The remote MCP endpoint uses standard MCP JSON-RPC over Streamable HTTP. That
means installation snippets, request envelopes, tool arguments, and discovery
metadata still use JSON where MCP clients expect it.

Tool result `text` fields and `docs://` resource bodies are encoded as compact
TOON to reduce model-facing token usage while preserving the same structured
data model. Resource contents are advertised with `mimeType: text/toon`.

Compact MCP payloads remove empty fields, avoid repeated raw metadata, and use
short readable keys such as `sig`, `uri`, `params`, `returns`, and `guides`.
Tools default to compact responses. Pass `detail: "full"` in a tool request only
when you need the larger raw field set.

For example, `resolve_symbol` returns TOON text like this:

```toon
matches[2]:
  - match: exact_name
    id: "T:Sandbox.Component"
    kind: class
    name: Sandbox.Component
    score: 110
    sig: public abstract class Sandbox.Component
    summary: "A GameObject can have many components, which are the building blocks of the game."
  - match: prefix_name
    id: "T:Editor.ComponentEditorWidget"
    kind: class
    name: Editor.ComponentEditorWidget
    score: 88
    sig: "public abstract class Editor.ComponentEditorWidget : Editor.Widget"
query: Component
returned: 2
```

## API Docs and Guides

The MCP server exposes two connected documentation layers:

- Generated API docs for namespaces, types, and members
- Official Guides fetched live from the s&box docs repository

The relationship is built at runtime. The server scans the upstream guide
markdown, looks for symbol mentions in headings, prose, inline code, and code
blocks, and then scores which guide pages are most relevant to each API symbol.

That gives agents two useful directions:

1. From an API symbol like `Sandbox.Component.OnUpdate()`, they can follow
   related Guides for conceptual context, editor workflows, and practical usage
   patterns.
2. From a Guide page, they can inspect the related API symbols that the guide
   discusses and then open the canonical type or member pages.

Use the API docs for exact contracts. Use Guides for surrounding context.

## Tool Reference

Tool responses are returned as TOON text to reduce token usage while preserving
the same structured data model. The examples below show tool arguments as JSON
because MCP clients send tool arguments through JSON-RPC.

Every tool accepts an optional `detail` field:

```json
{
  "detail": "full"
}
```

Omit it for compact responses. Use `detail: "full"` only when the compact result
does not include a field you need.

### `search_docs`

Use this first. It performs keyword or semantic search across the indexed s&box
API docs.

Typical input:

```json
{
  "query": "component update loop",
  "kind": "method",
  "typeName": "Sandbox.Component",
  "limit": 5
}
```

Typical TOON output includes:

- Ranked results
- Each result includes a structured symbol summary
- Common fields include symbol id, fully-qualified name, kind, signature, URL,
  score, and summary text

Example use:

- Find methods related to `Sandbox.Component`
- Search for `Ray`, `SceneTrace`, or `GameObject` by concept instead of exact id
- Use the best result with `explain_symbol_context` before answering usage
  questions

### `resolve_symbol`

Use this when a type name is short, incomplete, or ambiguous. It resolves names
to fully-qualified API types.

Typical input:

```json
{
  "name": "Component",
  "limit": 5
}
```

Typical TOON output includes:

- Matching types only
- Fully-qualified names such as `Sandbox.Component`
- Type metadata such as namespace, kind, and symbol id

Example use:

- Resolve `Component` before inspecting members
- Narrow `Scene` or `Transform` to the exact s&box type you need

### `explain_symbol_context`

Use this after `search_docs` or `resolve_symbol` when the user asks what a
symbol is, how to use an API, or needs enough context to choose the next lookup.
It intentionally returns a compact envelope instead of loading full guide pages.

Typical input:

```json
{
  "symbol": "Sandbox.Component",
  "memberLimit": 24
}
```

Typical TOON output includes:

- Symbol summary, signature, remarks, and declaration context
- Top related guide titles and `docs://guide/...` handles
- Compact type member handles when the symbol is a type
- `workflow.resource` for the canonical `docs://type/...` or
  `docs://member/...` page
- `workflow.next` recommendations such as `get_type_members`,
  `get_related_guides`, `get_method_details`, or `get_examples`

Example use:

- Ask "what is `NetworkMode`?" and receive the enum metadata plus related guide
  handles without loading full guide text
- Ask "how do I use `Sandbox.Component`?" and receive member and guide handles
  that point to the next focused lookup

### `get_symbol`

Use this to inspect one resolved symbol. It returns structured metadata for a
type or exact member reference.

Typical input:

```json
{
  "symbol": "Sandbox.Component"
}
```

Typical TOON output includes:

- Namespace and declaring type
- Symbol kind such as class, struct, enum, method, or property
- Signature and summary
- URLs, inheritance or declaration context when available
- `guideCount` and `workflow.next` when broader guide context exists

Example use:

- Read the summary for `Sandbox.Component`
- Inspect a property or exact member after resolving its symbol id
- Follow `workflow.next` to `get_related_guides` before explaining conceptual
  usage

### `get_type_members`

Use this after `get_symbol` when you want to browse what a type exposes.

Typical input:

```json
{
  "symbol": "Sandbox.Component",
  "kind": "method",
  "limit": 20
}
```

Typical TOON output includes:

- Member list for the target type
- Constructors, methods, or properties
- Structured rows with name, member kind, signature, summary, and symbol id

Example use:

- List lifecycle methods on `Sandbox.Component`
- Enumerate properties on a struct or class before asking about one specific member

### `get_method_details`

Use this when you already know the method you want and need the exact overload
details.

Typical input:

```json
{
  "symbol": "Sandbox.Component.OnUpdate()"
}
```

Typical TOON output includes:

- Exact method signature
- Parameters with names, types, and documentation
- Return type and return documentation
- Exceptions or related notes when indexed

Example use:

- Inspect `Sandbox.Component.OnUpdate()`
- Read the parameters for a trace, spawn, or utility method overload

### `get_examples`

Use this to fetch code examples attached to a symbol. This is useful when an
agent needs usage patterns instead of just signatures.

Typical input:

```json
{
  "symbol": "Sandbox.Component.OnUpdate()",
  "includeRelated": true
}
```

Typical TOON output includes:

- Direct examples for the symbol when available
- Optionally related examples from the declaring type
- Structured example snippets with source context

Example use:

- Get examples for a method override
- Fall back to type-level examples when a member has no dedicated sample

### `get_related_guides`

Use this when the agent already knows the symbol but needs broader context than
the generated API page provides.

Typical input:

```json
{
  "symbol": "Sandbox.Component.OnUpdate()",
  "limit": 5
}
```

Typical TOON output includes:

- Related Guide pages ranked for that symbol
- Match metadata showing whether the match is direct or inherited from the
  declaring type
- Resource URIs such as `docs://guide/scene/components`

Example use:

- Read workflow and editor guidance related to a method
- Move from `Sandbox.Component` or `Sandbox.Component.OnUpdate()` to the most
  relevant Guides before answering in prose

### `list_namespaces`

Use this to explore the API hierarchy from the top down.

Typical input:

```json
{
  "namespace": "Sandbox",
  "limit": 50
}
```

Typical TOON output includes:

- Child namespaces
- Types defined in the namespace
- Structured namespace and type summaries for navigation

Example use:

- Explore what lives under `Sandbox`
- Discover related namespaces before searching deeper

## Resources

The MCP server also exposes canonical `docs://` resources for agents that
already know the exact target they want to load. These resources complement the
tools:

- Use tools for discovery
- Use resources for deterministic page loading

Available resource patterns:

- `docs://schema`
- `docs://namespace/root`
- `docs://namespace/{name}`
- `docs://type/{full_name}`
- `docs://member/{full_name}`
- `docs://guide/{path}`

Examples:

```text
docs://namespace/Sandbox
docs://type/Sandbox.Component
docs://member/Sandbox.Component.OnUpdate
docs://guide/scene/components
```

Resources return compact TOON documentation pages. Resource wrappers omit
transport-only metadata such as the resource MIME type and repeated URI.
Depending on the resource, the payload can include:

- Summary and description
- Exact signatures and declarations
- Parameters, return types, and return documentation
- Examples
- Child namespaces, member listings, related guide links, and related API
  symbol links

Typical usage:

1. Use `search_docs` or `resolve_symbol` to identify the canonical symbol
2. Read the matching `docs://type/...`, `docs://member/...`, or
   `docs://guide/...` resource
3. Use the TOON resource payload as the canonical page for that API
   element or guide

## How Agents Use It

For a question like "How do I update a custom component every frame?", a well
behaved agent will usually:

1. Use `search_docs` with a query like `component update every frame`
2. Use `resolve_symbol` if it needs to confirm `Component` means `Sandbox.Component`
3. Use `explain_symbol_context` to get compact type, member, guide, and
   workflow hints
4. Use `get_type_members` to find lifecycle members such as `OnUpdate` if the
   compact member handles are not enough
5. Use `get_related_guides` when `workflow.next` recommends it for
   `Sandbox.Component` or `Sandbox.Component.OnUpdate()`
6. Read `docs://member/Sandbox.Component.OnUpdate` for the canonical API
   contract
7. Read a related `docs://guide/...` resource for broader workflow and usage
   context
8. Use `get_method_details` or `get_examples` when it needs an extra focused
   lookup

That is the intended MCP interaction model: search broadly, inspect the right
symbol, then combine exact API pages with related Guides when the answer needs
more than just signatures.
