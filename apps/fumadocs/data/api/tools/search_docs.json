{
  "name": "search_docs",
  "description": "Search official guides and all API symbols at once, including classes, enums, methods, constructors, and properties. Start here.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "detail": {
        "type": "string",
        "enum": ["compact", "full"],
        "description": "MCP response detail level. Defaults to compact; use full for a larger payload."
      },
      "query": {
        "type": "string",
        "description": "Keyword, symbol name, or natural-language documentation question."
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 20,
        "description": "Maximum number of mixed documentation results."
      },
      "includeGuides": {
        "type": "boolean",
        "description": "Include official guide pages."
      },
      "includeSymbols": {
        "type": "boolean",
        "description": "Include API symbols."
      }
    },
    "required": ["query"]
  },
  "execution": {
    "method": "POST",
    "path": "/api/v1/tools/search_docs"
  }
}
